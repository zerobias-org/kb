// No interactive dark mode toggle — theme is controlled by parent window via postMessage.
// The postMessage listener is in theme-init.js (loaded synchronously in <head>).
// This file is kept as a no-op so the Hugo template reference doesn't break.
